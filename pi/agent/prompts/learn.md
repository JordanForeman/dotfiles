---
description: Learn about a domain concept through interactive teaching
---
Use the `subagent` tool to delegate to the `code-explainer` agent:

{ "agent": "code-explainer", "task": "Teach the user about: $@. Start with fundamentals and progressively build understanding. Use real code examples from the codebase where relevant." }